; ═══════════════════════════════════════════════════════════════
;  modules.conf — IaaS-RonSys Central Telefónica (F2, Spec 05 §3.3)
;  autoload=yes: la imagen carga el set completo de módulos estándar
;  (res_pjsip*, chan_pjsip, app_mixmonitor, func_callerid, res_http/ari,
;  res_manager...). Mínimo y suficiente para pjsip + MixMonitor + AMI + ARI.
; ═══════════════════════════════════════════════════════════════

[modules]
autoload = yes
