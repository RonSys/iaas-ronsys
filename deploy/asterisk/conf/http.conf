; ═══════════════════════════════════════════════════════════════
;  http.conf — IaaS-RonSys Central Telefónica (F2, Spec 05 §3.3)
;  Servidor HTTP requerido por ARI (D4). D3: bind SOLO 127.0.0.1 —
;  el call-bridge corre en la misma máquina, NUNCA expuesto.
; ═══════════════════════════════════════════════════════════════

[general]
enabled = yes
bindaddr = 127.0.0.1
bindport = 8088
